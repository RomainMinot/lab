name: Node.js CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [22.18.0]
        # See supported Node.js release schedule

    steps:
    - uses: actions/checkout@v2
    - run: npm ci